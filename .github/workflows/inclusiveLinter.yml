name: Inclusive Terminology Linter

# Run this workflow every time a new commit pushed to your repository
on:
  pull_request:
    branches: [ main ]

jobs:
  terminology:
    runs-on: ubuntu-latest

    steps:
        # Checks out a copy of your repository on the ubuntu-latest machine
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Run simple action
        uses: actions/javascript-action@v1
        with:
          milliseconds: 1000
      - name: Comment Inclusive Check on new PR
        uses: BryanMThomas/inclusive-terminology-workflow@version-3
        with:
         message_id: 'Inclusive Check'
         pr_only: true
         GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
         
         
       #- name: Use Node.js
       # uses: actions/setup-node@v1
        #with:
         # node-version: 12.x
        # Runs Inclusive check and comments on PR
      #- name: Comment Inclusive Check on new PR
       # uses: BryanMThomas/inclusive-terminology-workflow@version-2
        #with:
         # message_id: 'Inclusive Check'
          #pr_only: false
        #env:
         # GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
